PORT=3000
OPENAI_API_KEY=CHAVE_OPENAI_AQUI
BOT_NAME=Atendente Santê
BUSINESS_NAME=Santê Moda
OPENAI_MODEL=gpt-4o-mini

# Diretório de sessões (defina para volume persistente em produção ex: /data/sessions)
SESS_DIR=./sessions

# Pareamento manual (1 = não auto-reconecta / força regenerar QR manualmente)
MANUAL_PAIRING=0

# Limites de QR antes de reset automático (apenas modo automático)
QR_MAX_BEFORE_RESET=8
QR_MAX_AGE_BEFORE_RESET=120000

# Supabase
SUPABASE_URL=https://nsasgjcpyijhkqiyyyez.supabase.co
SUPABASE_ANON_KEY=COLOQUE_A_CHAVE_ANON_AQUI
# (Opcional backend) Chave service role NUNCA expor no frontend
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_USERS_TABLE=users
